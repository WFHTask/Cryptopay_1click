# 快速开始指南

## 🚀 5分钟快速部署

### 第一步：修改API密钥（必须）

1. 打开 `docker-compose.yml` 文件

2. 搜索 `your_api_token_here_change_me`，找到两处：
   ```yaml
   # 第一处：Epusdt服务
   API_AUTH_TOKEN: your_api_token_here_change_me
   
   # 第二处：Backend服务
   EPUSDT_API_TOKEN: your_api_token_here_change_me
   ```

3. 将两处都改为相同的强密码，例如：
   ```yaml
   API_AUTH_TOKEN: abc123xyz789qwerty456
   EPUSDT_API_TOKEN: abc123xyz789qwerty456
   ```

### 第二步：启动服务

#### Windows用户：
```powershell
# 在项目根目录执行
docker-compose up -d --build
```

#### Linux/Mac用户：
```bash
# 给脚本添加执行权限
chmod +x start.sh

# 运行启动脚本
./start.sh
```

### 第三步：配置钱包地址

1. 等待服务启动完成（约30秒）

2. 访问 Epusdt 后台：http://localhost:8000

3. 点击"钱包管理"添加USDT TRC20钱包地址

   示例钱包地址格式：`TNEns8t9jbWENbStkQdVQtHMGpbsYsQjZK`

4. 至少添加一个钱包地址

### 第四步：测试支付

1. 访问前端页面：http://localhost:3000

2. 点击"生成"按钮生成订单号

3. 输入测试金额，例如：100

4. 点击"创建支付订单"

5. 查看生成的支付二维码和钱包地址

6. 使用USDT钱包扫码或转账到指定地址

## ✅ 检查服务状态

### 查看所有服务
```bash
docker-compose ps
```

正常状态应该显示：
```
NAME                 STATUS         PORTS
payment_backend      Up            0.0.0.0:8080->8080/tcp
payment_epusdt       Up            0.0.0.0:8000->8000/tcp
payment_frontend     Up            0.0.0.0:3000->80/tcp
payment_mysql        Up (healthy)  0.0.0.0:3306->3306/tcp
payment_redis        Up (healthy)  0.0.0.0:6379->6379/tcp
```

### 查看日志
```bash
# 查看所有日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f epusdt
docker-compose logs -f frontend
```

### 测试API健康状态
```bash
# 测试后端API
curl http://localhost:8080/api/health

# 应该返回：{"status":"ok"}
```

## 📱 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端页面 | http://localhost:3000 | 用户支付界面 |
| 后端API | http://localhost:8080 | API接口 |
| Epusdt后台 | http://localhost:8000 | 支付服务管理后台 |

## ❓ 常见问题

### 1. 端口被占用怎么办？

如果看到类似错误：`Bind for 0.0.0.0:3000 failed: port is already allocated`

解决方法：修改 `docker-compose.yml` 中的端口映射

```yaml
# 将冲突的端口改为其他端口
frontend:
  ports:
    - "3001:80"  # 将3000改为3001
```

### 2. 服务启动失败？

```bash
# 停止所有服务
docker-compose down

# 清理并重新启动
docker-compose up -d --build --force-recreate
```

### 3. 找不到钱包地址？

确保：
- 已在 Epusdt 后台添加钱包地址
- 钱包地址是 TRC20 格式
- 至少添加一个钱包地址

### 4. 支付后没有回调？

检查：
```bash
# 查看后端日志
docker-compose logs -f backend

# 查看 Epusdt 日志
docker-compose logs -f epusdt
```

确认 API Token 配置是否一致。

### 5. MySQL初始化失败？

```bash
# 停止服务并删除数据卷
docker-compose down -v

# 重新启动
docker-compose up -d
```

## 🛑 停止服务

### Windows用户：
```powershell
docker-compose down
```

### Linux/Mac用户：
```bash
./stop.sh
```

### 完全清理（包括数据）：
```bash
docker-compose down -v
```

⚠️ 注意：`-v` 参数会删除所有数据，包括数据库数据！

## 📊 性能优化建议

### 1. 增加钱包地址数量
在 Epusdt 后台添加多个钱包地址，提高并发支付能力

### 2. 调整订单过期时间
修改 `docker-compose.yml`：
```yaml
epusdt:
  environment:
    - ORDER_EXPIRATION_TIME=15  # 改为15分钟
```

### 3. 生产环境优化
```yaml
backend:
  environment:
    - GIN_MODE=release  # 关闭调试模式
```

## 🔒 安全提示

1. ✅ 修改默认API密钥
2. ✅ 修改MySQL密码
3. ✅ 不要暴露不必要的端口
4. ✅ 定期备份数据
5. ✅ 使用HTTPS（生产环境）

## 📖 下一步

- 阅读 [README.md](README.md) 了解详细文档
- 阅读 [配置说明.md](配置说明.md) 了解配置选项
- 查看 [API文档](README.md#api文档) 进行接口集成

## 🆘 获取帮助

如果遇到问题：
1. 查看日志：`docker-compose logs -f`
2. 检查配置：`docker-compose config`
3. 重启服务：`docker-compose restart`
4. 查看文档：README.md 和 配置说明.md

## 🎉 开始使用

现在你可以：
1. 在前端创建支付订单
2. 扫码或复制地址进行USDT支付
3. 查看支付结果
4. 通过API集成到你的系统

祝使用愉快！🚀

