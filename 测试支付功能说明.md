# 🧪 测试支付功能使用指南

## 功能概述

商户测试支付功能允许商户在管理后台直接创建测试订单，获取支付二维码和支付地址，方便快速测试和调试支付流程。

## 安装依赖

在使用测试功能前，需要安装新的依赖：

```powershell
cd frontend
npm install qrcode
```

或者重新构建Docker容器：

```powershell
docker-compose up -d --build
```

## 访问入口

### 方式一：通过仪表盘
1. 登录商户后台：http://localhost:3000/merchant/login
2. 进入仪表盘
3. 点击"快速操作"区域的"🧪 测试支付"按钮

### 方式二：通过侧边栏
1. 登录商户后台
2. 在左侧导航栏点击"🧪 测试支付"

### 方式三：直接访问
http://localhost:3000/merchant/test

## 功能特性

### 1. 创建测试订单
- **支付类型选择**：支持标准支付和嵌入式支付两种模式
- **自定义金额**：支持输入任意金额（CNY）
- **快速金额**：提供¥10、¥100、¥500、¥1000快速选择
- **订单号自动生成**：系统自动生成唯一订单号，无需手动输入 ⭐ NEW
- **回调配置**：可设置支付成功后的回调地址
- **返回地址**：可设置用户支付后的跳转地址

### 2. 支付信息展示
- **二维码显示**：自动生成USDT支付二维码（霓虹绿风格）
- **订单信息**：显示系统订单号、商户订单号
- **金额信息**：显示人民币金额和对应的USDT金额
- **收款地址**：显示完整的USDT-TRC20收款地址
- **一键复制**：所有重要信息都支持一键复制

### 3. 快速操作
- **打开支付页面**：在新标签页打开支付页面，模拟用户支付
- **创建新订单**：快速清空结果，创建新的测试订单

### 4. 嵌入式支付（NEW!）
- **iframe代码生成**：自动生成可嵌入的iframe代码
- **嵌入URL**：提供直接访问的嵌入式支付URL
- **实时预览**：在测试页面直接预览嵌入效果
- **一键复制**：复制嵌入代码到您的网站

### 5. API示例
- 实时显示API调用代码示例
- 自动填充当前表单的参数
- 方便开发者参考集成

## 使用流程

### 步骤1：确保已添加钱包
```
仪表盘 → 钱包管理 → 添加钱包
```
至少添加一个USDT-TRC20收款地址

### 步骤2：选择支付类型
- **💳 标准支付**：独立的支付页面，适合跳转场景
- **🎯 嵌入式支付**：可嵌入到您网站的支付组件

### 步骤3：创建测试订单
1. 选择或输入支付金额
2. （可选）设置回调地址和返回地址
3. 点击"🚀 创建测试订单"
4. 订单号由系统自动生成，格式：`TEST_{时间戳}_{随机字符}`

### 步骤4：获取支付信息

#### 标准支付模式
- 查看生成的二维码
- 复制收款地址
- 记录订单号
- 点击"📱 打开支付页面"查看用户视角

#### 嵌入式支付模式
- 查看嵌入URL和iframe代码
- 复制iframe代码到您的网站
- 在预览区域查看实际效果
- 点击"🎯 打开嵌入式页面"测试独立访问

### 步骤5：模拟支付
- 使用USDT钱包扫码支付（真实支付）
- 或在后台订单列表中查看订单状态

### 步骤6：验证结果
```
仪表盘 → 订单列表 → 查看刚创建的订单
```

## 界面说明

### 左侧区域 - 创建订单
- **支付类型选择**：标准支付 / 嵌入式支付
- **订单表单**：填写金额和回调地址（订单号自动生成）
- **API示例**：显示实际的API调用代码

### 右侧区域 - 支付结果

#### 标准支付显示
- **二维码**：300x300像素，霓虹绿边框
- **订单详情**：完整的支付信息
- **操作按钮**：打开支付页面 / 创建新订单

#### 嵌入式支付显示
- **二维码**：基础支付信息
- **嵌入URL**：可直接访问的链接
- **iframe代码**：可复制的HTML代码
- **实时预览**：450x650像素的iframe预览
- **操作按钮**：打开嵌入式页面 / 创建新订单

## 测试建议

### 开发环境测试
1. 使用小额测试（建议¥10-¥100）
2. 先测试订单创建流程
3. 再测试实际支付流程
4. 检查回调通知是否正常

### 集成测试
1. 复制API示例代码
2. 在自己的系统中调用API
3. 验证订单创建返回数据
4. 测试回调接口接收

### 压力测试
1. 批量创建多个订单
2. 观察系统响应时间
3. 检查订单分配是否均匀（如果有多个钱包）

## 嵌入式支付使用

### 1. 获取嵌入代码
创建订单后，系统会自动生成：
- **嵌入URL**：可直接在浏览器打开的链接
- **iframe代码**：可嵌入到HTML页面的代码

### 2. 嵌入到您的网站

```html
<!-- 方式1：直接粘贴iframe代码 -->
<iframe 
  src="http://localhost:3000/embed/payment/xxxxx?..." 
  width="450" 
  height="650" 
  frameborder="0" 
  scrolling="no"
  style="border: 2px solid rgba(0,255,159,0.3); border-radius: 12px;">
</iframe>

<!-- 方式2：动态创建 -->
<div id="payment-container"></div>
<script>
  const container = document.getElementById('payment-container');
  container.innerHTML = '您的iframe代码';
</script>
```

### 3. 响应式调整

```html
<!-- 适配移动端 -->
<iframe 
  src="..." 
  width="100%" 
  height="650" 
  style="max-width: 450px; ...">
</iframe>
```

### 4. 完整示例

详见项目根目录：`嵌入式支付使用指南.md` 和 `embed-demo.html`

## API调用示例

### Node.js
```javascript
const axios = require('axios');

const createPayment = async () => {
  try {
    const response = await axios.post('http://localhost:8080/api/payment/create', {
      amount: 100,
      order_id: `TEST_${Date.now()}`,
      notify_url: 'https://your-domain.com/callback',
      return_url: 'https://your-domain.com/success',
      api_key: 'your_api_key_here'
    });
    
    console.log('支付地址:', response.data.data.token);
    console.log('USDT金额:', response.data.data.actual_amount);
  } catch (error) {
    console.error('创建失败:', error.message);
  }
};

createPayment();
```

### Python
```python
import requests
import time

def create_payment():
    url = 'http://localhost:8080/api/payment/create'
    data = {
        'amount': 100,
        'order_id': f'TEST_{int(time.time() * 1000)}',
        'notify_url': 'https://your-domain.com/callback',
        'return_url': 'https://your-domain.com/success',
        'api_key': 'your_api_key_here'
    }
    
    response = requests.post(url, json=data)
    result = response.json()
    
    if result['success']:
        print('支付地址:', result['data']['token'])
        print('USDT金额:', result['data']['actual_amount'])
    else:
        print('创建失败:', result['message'])

create_payment()
```

### PHP
```php
<?php
$url = 'http://localhost:8080/api/payment/create';
$data = [
    'amount' => 100,
    'order_id' => 'TEST_' . time(),
    'notify_url' => 'https://your-domain.com/callback',
    'return_url' => 'https://your-domain.com/success',
    'api_key' => 'your_api_key_here'
];

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, 1);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Content-Type: application/json'
]);

$response = curl_exec($ch);
curl_close($ch);

$result = json_decode($response, true);
if ($result['success']) {
    echo '支付地址: ' . $result['data']['token'] . "\n";
    echo 'USDT金额: ' . $result['data']['actual_amount'] . "\n";
} else {
    echo '创建失败: ' . $result['message'] . "\n";
}
?>
```

## 常见问题

### Q: 创建订单失败，提示"请先添加钱包"
**A:** 需要先在"钱包管理"页面添加至少一个USDT-TRC20收款地址

### Q: 二维码不显示
**A:** 检查浏览器控制台错误，确保已安装qrcode库

### Q: 嵌入式支付预览显示空白
**A:** 检查是否已成功创建订单，确认嵌入URL是否正确生成

### Q: 点击"打开嵌入式页面"没有反应
**A:** 确保已成功创建订单，按钮在URL生成后才会启用

### Q: iframe无法在我的网站显示
**A:** 检查CSP（Content Security Policy）设置，确保允许iframe嵌入

### Q: 看不到订单号输入框
**A:** 订单号由系统自动生成，无需手动输入，创建成功后可在结果中查看

### Q: 订单创建成功但找不到
**A:** 在"订单列表"页面可以看到所有创建的订单

### Q: 回调地址如何测试
**A:** 可以使用ngrok等工具将本地服务暴露到公网，或者部署到测试服务器

### Q: 支付后多久更新状态
**A:** 实时监听区块链，通常1-3分钟内确认（取决于区块链网络）

## 注意事项

⚠️ **重要提示**
- 测试环境请使用小额进行测试
- 所有支付都是真实的USDT转账，无法退款
- 请妥善保管API密钥，不要泄露
- 生产环境部署时请修改回调地址为实际地址

✅ **最佳实践**
- 先在测试环境充分测试
- 使用唯一的订单号，避免重复
- 回调接口做好幂等性处理
- 记录所有订单和回调日志

## 功能截图

### 测试支付页面结构
```
┌──────────────────────────────────────────────────────────┐
│  💳 测试支付功能                                          │
│  在这里测试支付订单创建、获取支付码等功能                 │
├─────────────────────┬────────────────────────────────────┤
│  创建测试订单       │  订单创建成功                       │
│  ┌──────────────┐   │  ┌──────────────┐                 │
│  │💳标准 🎯嵌入│   │  │  [二维码]    │                 │
│  └──────────────┘   │  └──────────────┘                 │
│  ┌──────────────┐   │  系统订单号: xxxxx                 │
│  │ 金额输入     │   │  商户订单号: TEST_17xx_abc         │
│  │ ¥10 ¥100    │   │  支付金额: ¥100                    │
│  └──────────────┘   │  USDT金额: 14.23 USDT             │
│  ┌──────────────┐   │  收款地址: TXxxx... [📋]           │
│  │ 回调地址     │   │                                    │
│  └──────────────┘   │  【嵌入式支付特有】                │
│  🚀 创建测试订单    │  嵌入URL: http://... [📋]         │
│                     │  iframe代码: <iframe... [📋]      │
│  📝 API调用示例     │  ┌──────────────┐                 │
│  ┌──────────────┐   │  │ [预览窗口]   │                 │
│  │  POST /api/  │   │  │   450x650    │                 │
│  │  payment/... │   │  └──────────────┘                 │
│  └──────────────┘   │  ┌──────────────────────┐         │
│  💡 订单号自动生成  │  │ 🎯 打开嵌入式页面     │         │
│                     │  │ 🔄 创建新订单         │         │
│                     │  └──────────────────────┘         │
│                     │  💡 测试提示（根据类型不同）       │
└─────────────────────┴────────────────────────────────────┘
```

## 更新日志

### v1.3.0 (2025-10-14)
- ✅ 完全移除订单号输入框
- ✅ 订单号由系统自动生成，确保唯一性
- ✅ 简化创建流程，提升用户体验

### v1.2.0 (2025-10-14)
- ✅ 修复点击"打开嵌入式页面"无反应的问题
- ✅ 订单号改为默认自动生成
- ✅ 新增自动刷新订单号开关
- ✅ 优化按钮状态和错误提示
- ✅ 添加URL验证机制

### v1.1.0 (2025-10-14)
- ✅ 新增嵌入式支付测试功能
- ✅ 支持支付类型切换（标准/嵌入式）
- ✅ 自动生成iframe嵌入代码
- ✅ 实时预览嵌入效果
- ✅ 一键复制嵌入代码

### v1.0.0 (2025-10-14)
- ✅ 初始版本发布
- ✅ 支持订单创建和二维码生成
- ✅ 支持快速金额选择
- ✅ 支持订单号随机生成
- ✅ 显示API调用示例
- ✅ 添加测试提示和功能说明

## 技术支持

如有问题，请查看：
- 📖 项目文档：`快速开始.md`
- 📋 完整功能：`完整功能清单.md`
- 🔧 配置说明：`配置说明.md`

---

**开始测试您的支付功能吧！** 🚀

